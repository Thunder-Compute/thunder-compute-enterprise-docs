---
title: "Kubernetes internals"
sidebarTitle: "Kubernetes internals"
description: "How the Thunder Helm chart is put together, and the Kubernetes abstractions it builds on."
icon: "diagram-project"
iconType: "solid"
---

What the chart runs in the cluster, the abstractions it relies on, and why claims are
modelled differently for pods and VMs. For installation steps see
[Kubernetes](/kubernetes).

## Components

![Thunder Device Plugin Kubernetes Architecture](/images/thunder-device-plugin-helm.png)

**Operator** (Deployment) — reads live GPU capacity and usage from the Thunder API
and publishes it as Kubernetes inventory:

- one `ResourceSlice` device per GPU, in one pool per zone and GPU type
- one `DeviceClass` per GPU type, each exposing its own extended resource name

**Daemonset** (GPU nodes) — turns an allocation into a working GPU:

- sets up Thunder virtualization on the node and monitors its health
- creates a `client.thundercompute.com` resource, `Secret`, and `ConfigMap` per `ResourceClaim`
- performs CDI injection for container consumers
- tears down and frees GPU resources when a pod or VM goes away

It builds on two Kubernetes abstractions,
[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
and [Container Device Interface (CDI)](https://docs.docker.com/build/building/cdi/).

## Dynamic Resource Allocation

| Resource | Used for |
| --- | --- |
| `DeviceClass` | A category of device. Thunder publishes exactly one per GPU model. |
| `ResourceSlice` | The devices available in the cluster. Thunder publishes one device per GPU, pooled per zone and model. |
| `ResourceClaimTemplate` | A request for GPUs of one model. Kubernetes generates one claim per pod from it. |
| `ResourceClaim` | A concrete claim attached to a consumer; represents GPUs in use. |

### One device per GPU

Each GPU is its own device, so the number of GPUs in a request is a device **count**:

```yaml
count: 2        # two GPUs
```

This matters beyond ergonomics. Kubernetes translates an extended resource request
(`thundercompute.com/gpu-a6000: 2`) into a count of devices, so modelling a GPU as a
device is what lets both request styles resolve against the same pool.

A slice holds at most 128 devices, so a zone with more GPUs of one model is sharded
across several slices that share a pool name and generation.

### Pooling and node selection

A pool is scoped to a zone through the slice's node selector, not to a node. Any node
in the zone can consume from it, which is why Thunder GPUs never appear in a node's
`allocatable` — the scheduler resolves them instead of the kubelet advertising them.

### Oversubscription

The number of GPUs a pool publishes is the physical count scaled by the zone's
oversubscription target, which the operator reads from the Thunder API per GPU
model. Four physical GPUs at a `1.5` target are published as six devices.

A claim always gets whole GPUs; sharing is expressed by how many devices the zone
publishes. Targets round down so a target is never exceeded, and a missing or
unreadable target falls back to `1` rather than emptying a zone.

### Why GPU types are separate classes

A Thunder client is enrolled with exactly one GPU model, so every claim has to
resolve to a single model. A `DeviceClass` carries per-device selectors, so publishing
one class per model puts the model in both the class name and the extended resource
name, and a request names it either way. See
[Naming a GPU type](/kubernetes#naming-a-gpu-type).

### Inventory drives the API surface

Classes and pools are generated from Thunder inventory on every reconcile, so
enrolling a node with a new GPU model creates its class, pool, and resource name
without a chart upgrade, and retiring the last one removes them.

The GPU model is never taken from a Kubernetes label. `thunderd` detects the
hardware on the host when the daemon enrolls it, reports it to the Thunder API, and
the operator reads it back:

```text
thunderd → Thunder API {gpuType, gpuCount} → operator → DeviceClass + ResourceSlice
```

The daemon's own `nvidia-smi` checks are a gate, not a source of truth: they confirm
a GPU exists and the driver is recent enough before enrolling, and never read the
model. A node wrongly marked as a Thunder node therefore fails at startup with a
clear error.

## Container Device Interface

CDI provides `containerEdits`, which modify the environment variables, devices, and
mounts a container sees. Thunder uses it to inject NVIDIA libraries and executables,
environment variables, and hooks, so an unmodified CUDA image runs against a remote
GPU without being rebuilt.

The libraries the host already has — `libcuda.so.1`, `libnvidia-ml.so.1` and
`nvidia-smi` — are read-only bind mounts. No GPU device nodes are injected, because
the GPU is not local.

`libthunder.so` arrives through a `createRuntime` hook the daemon adds to each claim's
CDI spec. The hook runs on the node while the container is being created, before it
starts, in the runtime's namespaces — where the node's network is reachable — and
writes into the container's filesystem at `<bundle>/rootfs`:

1. Resolves the container root from the OCI state the runtime passes on stdin.
2. Ensures the node has the pinned `libthunder.so`, downloaded once and cached by
   digest.
3. Exchanges the claim's single-use enrollment token for the client's auth token and
   claims, caching the result so a container restart re-stages without re-enrolling.
4. Writes `/etc/thunder/libthunder.so`, `/etc/thunder/config.json`, and a CA bundle
   if the image ships none.

The exchange happens here, at container create, because an enrollment token is single
use: a VM claim that never starts a container keeps an unspent token in its guest
Secret.

The hook is why workload images need nothing added to them, and why an image is never
pinned to a Thunder client version.

## ResourceClaim vs ResourceClaimTemplate

| | Use | Why |
| --- | --- | --- |
| Pods | `ResourceClaimTemplate` | Kubernetes generates and owns one claim per pod, and deletes it with the pod. |
| VMs | `ResourceClaim` | The claim name must be known when the manifest is written, because it names the guest Secret the VM mounts. |

A stable claim name is what lets a VM mount the artifacts the daemon generates for it
(see [KubeVirt](/kubevirt)):

```text
<claim-name>-thunder-setup   # enrollment-token, install-thunder-client.sh
```

A standalone `ResourceClaim` is safe for VMs: it is not bound to a `ResourceSlice`,
pool, zone, or node until the scheduler processes the consuming virt-launcher pod.
